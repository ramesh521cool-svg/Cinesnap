// ─────────────────────────────────────────────────────────────────
// CineSnap — Production API Configuration
//
// After deploying the backend to Render, paste your Render URL below.
// Example: https://cinesnap-api.onrender.com
//
// This file is loaded BEFORE index.html's <script> block, so
// window.CINESNAP_API is available immediately.
// ─────────────────────────────────────────────────────────────────
// Same origin — frontend and API are served from the same Railway URL.
// Empty string means "use current host" (works locally AND in production).
window.CINESNAP_API = '';
